# Docker images

Application Dockerfiles will be introduced alongside their runtimes in Phases 2 and 4. The root Compose file already provisions version-pinned PostgreSQL/pgvector and Redis dependencies.
